[pytest]
testpaths = tests
norecursedirs = docs deprecated scratch .claude .secrets node_modules bin obj
python_files = test_*.py automation_test_*.py
markers =
    slow: marks tests that run the full audit pipeline (exclude with -m 'not slow')
